<?php

/**
 * 福利券和体验券配置
 *
 * 配置说明：
 * - 体验券：新会员注册后可领取
 * - 福利券：根据昨日/今日打码量领取
 */

return [

    // ============================================================
    // 活动时间配置
    // ============================================================
    'activity' => [
        // 活动结束时间（到达此时间后，福利券和体验券暂停发放）
        // 格式：Y-m-d H:i:s
        'end_time' => '2026-12-01 08:00:00',

        // 是否强制开启（忽略结束时间限制，用于测试）
        'force_enable' => false,
    ],

    // ============================================================
    // 体验券配置
    // ============================================================
    'experience' => [
        // 是否启用体验券功能
        'enabled' => true,

        // 新用户注册时间阈值（大于等于此时间注册的用户视为新用户）
        // 格式：Y-m-d H:i:s
        'register_after' => '2026-01-01 00:00:00',

        // 每天可领取次数
        'daily_limit' => 1,

        // 总可领取次数
        'total_limit' => 5,

        // 每次领取的分数
        'score' => 1000,

        // 领取后是否自动出票（打印）
        'auto_print' => true,

        // 有效时间（小时），超过此时间后券自动失效
        'expire_hours' => 24,
    ],

    // ============================================================
    // 福利券配置
    // ============================================================
    'welfare' => [
        // 是否启用福利券功能
        'enabled' => true,

        // 每天可领取次数（0表示不限制）
        'daily_limit' => 1,

        // 打码量规则配置（昨日规则）
        // 格式：['bet_amount' => 最低打码量, 'score' => 可领取分数]
        // 按打码量从低到高排序，系统会自动匹配最高可领取的档位
        'rules' => [
            [
                'bet_amount' => 100000,  // 昨日打分达10万分
                'score' => 1000,         // 可领取福利券1000分
            ],
            [
                'bet_amount' => 300000,  // 昨日打分达30万分
                'score' => 2000,         // 可领取福利券2000分
            ],
            [
                'bet_amount' => 500000,  // 昨日打分达50万分
                'score' => 3000,         // 可领取福利券3000分
            ],
        ],

        // 领取后是否自动出票（打印）
        'auto_print' => true,

        // 有效时间（小时），超过此时间后券自动失效
        'expire_hours' => 24,
    ],

    // ============================================================
    // 今日打码量特殊规则（福利券）
    // ============================================================
    'today_welfare' => [
        // 是否启用今日打码量福利券
        'enabled' => true,

        // 今日打码量规则
        'rules' => [
            [
                'bet_amount' => 200000,  // 今日打分达20万分
                'score' => 1000,         // 可立即领取福利券1000分
            ],
        ],
    ],

    // ============================================================
    // 订单号前缀配置
    // ============================================================
    'order_prefix' => [
        'experience' => 'TY',  // 体验券订单号前缀
        'welfare' => 'FL',     // 福利券订单号前缀
        'recharge' => 'TK',    // 开分订单号前缀
        'withdraw' => 'TK',    // 洗分订单号前缀
    ],
];
